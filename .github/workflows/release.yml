name: Build Release DMG

on:
  push:
    tags:
      - "v*"

permissions:
  contents: write

jobs:
  release:
    runs-on: macos-15
    steps:
      - uses: actions/checkout@v4
      - name: Build DMG
        run: ./scripts/build-dmg.sh
      - name: Publish GitHub release
        env:
          GH_TOKEN: ${{ github.token }}
        run: |
          versioned_dmg="$(find dist -maxdepth 1 -name 'Taskbar-S-*.dmg' ! -name 'Taskbar-S.dmg' -print -quit)"
          if gh release view "$GITHUB_REF_NAME" >/dev/null 2>&1; then
            gh release upload "$GITHUB_REF_NAME" "$versioned_dmg" dist/Taskbar-S.dmg dist/SHA256SUMS.txt --clobber
          else
            gh release create "$GITHUB_REF_NAME" "$versioned_dmg" dist/Taskbar-S.dmg dist/SHA256SUMS.txt --generate-notes --title "Taskbar S $GITHUB_REF_NAME"
          fi
